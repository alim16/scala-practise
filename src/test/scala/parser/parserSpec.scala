import org.scalatest._
import parser.{doStuff => parseStuff}

class ParserSpec extends FlatSpec{

    it should "match something" in {
        //assert(parseStuff() == true)
        assert(true==true)
    }

}